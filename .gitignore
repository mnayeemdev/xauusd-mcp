node_modules/
screenshots/
scripts/current.pine
temp_*
*.log
discovery-log.json
rules.json

# XAUUSD auto signal watcher — local runtime bookkeeping (last processed
# candle, last alerted signal, single-instance lock), not a trading ledger.
state/
